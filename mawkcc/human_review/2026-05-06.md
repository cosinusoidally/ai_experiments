# 2026-05-06 notes for mawkcc

I'm probably not going to do much, if any, dev work today, but I am going to
write down the things I intend to do tomorrow:

* review the issues in the main README and start looking at fixes
* I should maybe get Codex to look at adding debug info to the files. If I do
  that it will probablly be after I look at the other bugs myself, I don't want
  to end up with it changing the code under my feet as I'm reviewing it. I think
  I should try and make the debugging optional via shims. eg suppose there was
  a debug.c and debug_stubs.c. The current code would then call the relevant
  routines, with dummy stub calls in the stub implementation and the real code
  in the debug.c file. I think this would be the least invasive way to add in
  the debug code as it avoids mingling the debug code in with the current code.
