Next mawkcc_self.c porting steps
=================================

Remove a step from this file after it is completed.

Line counts are estimates of implementation lines that can be removed
from mawkcc_ansi.c when the step is completed. The major parser, lexer,
object builder, executable builder, string registration, code_reset,
source-file reading, final binary emission, low-level code/binary
emission, binary reset, and self-owned state-array/buffer work have
already been completed.

No currently listed simple porting steps remain. The remaining ANSI-side
code is mostly process/libc/error-reporting support: allocation wrappers,
`failf`/`fail_code`, `u32_byte`, `usage`, and global state definitions used
by the split and pure-GCC builds.
