--- a/src/system.h
+++ b/src/system.h
@@ -25,15 +25,15 @@
-#if __GNUC__
-# define alloca __builtin_alloca
-#else
-# if HAVE_ALLOCA_H
-#  include <alloca.h>
-# else
-#  ifdef _AIX
- #pragma alloca
-#  else
-#   ifndef alloca
+#if HAVE_ALLOCA_H
+# include <alloca.h>
+#else
+# if __GNUC__
+#  define alloca __builtin_alloca
+# else
+#  ifdef _AIX
+ #pragma alloca
+#  else
+#   ifndef alloca
 char *alloca ();
-#   endif
-#  endif
-# endif
-#endif
+#   endif
+#  endif
+# endif
+#endif
