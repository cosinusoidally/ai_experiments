/* Mozilla's imported Test262 shell.js calls this harness hook after loading.
 * An uncaught exception already reaches test262_runner.js as a failed result,
 * so successful evaluation needs no additional registration. */
function testPassesUnlessItThrows() {
}
